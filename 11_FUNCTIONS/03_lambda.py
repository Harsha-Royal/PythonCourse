square = lambda x: x ** 2
print(square(5))  # Output: 25

sum = lambda a, b: a + b
print(sum(3, 4))  # Output: 7


# lambda functions are anonymous functions that can take any number of arguments but can only have one expression. They are often used for short, simple functions that are not reused elsewhere in the code.
